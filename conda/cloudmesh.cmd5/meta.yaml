package:
  name: cloudmesh.cmd5
  version: 1.0

source:
  git_url: https://github.com/cloudmesh/cloudmesh.cmd5.git

build:
  number: 0

requirements:
  build:
    - python
    - pyyaml
    - docopt
    - requests

  run:
    - pyyaml
    - cloudmesh.common
    - docopt
    - requests
